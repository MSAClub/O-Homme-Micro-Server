server:
  port: 0
  address: 0.0.0.0

spring:
  application:
    name: review
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/msa?serverTimezone=UTC&useUniCode=yes&characterEncoding=UTF-8
    username: ssafy
    password: ssafy


mybatis:
  type-aliases-package: com.msaclub.review.model.mapper
  mapper-locations: classpath:mybatis/mapper/*.xml



eureka:
  instance:
    instance-id: ${spring.application.name}:${spring.application.instance_id:${random.value}}
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://3.143.74.33:8761/eureka

